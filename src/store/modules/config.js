const state = {
  isMobile: false,
};

const getters = {};

const actions = {};

const mutations = {
  setIsMobile: (state, isMobile) => (state.isMobile = isMobile)
};

export default {
  state,
  getters,
  actions,
  mutations,
}